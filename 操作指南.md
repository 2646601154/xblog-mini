# Xblog-mini 部署操作指南

## 一、域名解析配置

在域名服务商（如阿里云/腾讯云）后台添加 DNS 解析：

| 记录类型 | 主机记录 | 记录值 | TTL |
|---------|---------|--------|-----|
| A | xblog | 101.34.249.12 | 600 |
| A | xadmin | 101.34.249.12 | 600 |

配置后等待生效（通常几分钟），验证方式：

```bash
# 本地验证
nslookup xblog.xiaruoxin.cn
nslookup xadmin.xiaruoxin.cn
```

---

## 二、服务器首次部署

### 1. SSH 登录服务器

```bash
ssh ubuntu@101.34.249.12
```

### 2. 安装 Docker Compose

```bash
sudo apt update
sudo apt install docker-compose -y
```

### 3. 克隆项目代码

```bash
sudo mkdir -p /opt/xblog
sudo chown ubuntu:ubuntu /opt/xblog
cd /opt/xblog
git clone git@gitee.com:empty-summer/xblog.git .
```

### 4. 创建 .env 环境变量文件

```bash
cat > .env << 'EOF'
DB_PASSWORD=wX9xV2pJ8q
JWT_SECRET=eP4aG3iK8mJ1nF4p
EOF
```

### 5. 构建并启动所有服务

```bash
docker-compose up -d --build
```

### 6. 验证服务状态

```bash
# 查看容器状态
docker-compose ps

# 查看 API 日志（确认启动成功）
docker-compose logs -f api

# 查看 MySQL 日志
docker-compose logs -f mysql
```

正常输出应包含：
- `xblog-mysql` - running (healthy)
- `xblog-api` - running
- `xblog-nginx` - running

### 7. 浏览器访问验证

- 展示端：http://xblog.xiaruoxin.cn
- 管理端：http://xadmin.xiaruoxin.cn

### 8. 初始化测试数据（可选）

```bash
# 进入 MySQL 容器导入测试数据
docker exec -i xblog-mysql mysql -uroot -pwX9xV2pJ8q xblog < sql/test-data.sql
```

---

## 三、日常更新部署

### 方式一：手动部署（推荐新手）

```bash
# SSH 登录服务器
ssh ubuntu@101.34.249.12

# 进入项目目录
cd /opt/xblog

# 拉取最新代码
git pull origin main

# 重新构建并启动（仅重建有变化的服务）
docker-compose up -d --build
```

### 方式二：自动部署（GitHub Actions）

配置完成后，push 代码到 GitHub 会自动触发构建和部署。

#### 首次配置

1. 本地生成 SSH 密钥（如果没有）：

```bash
ssh-keygen -t rsa -b 4096 -C "你的邮箱"
cat ~/.ssh/id_rsa
```

2. 在 GitHub 仓库添加 Secrets：

   - 进入仓库 → Settings → Secrets and variables → Actions → New repository secret
   - 添加以下密钥：

| 密钥名 | 值 | 说明 |
|--------|-----|------|
| `SSH_PRIVATE_KEY` | `~/.ssh/id_rsa` 的内容 | 服务器 SSH 私钥 |
| `SERVER_HOST` | `101.34.249.12` | 服务器 IP |
| `SERVER_USER` | `ubuntu` | SSH 用户名 |

3. 服务器上配置 GitHub SSH 密钥：

```bash
# 在服务器上执行，确保能拉取 GitHub 仓库
ssh-keyscan github.com >> ~/.ssh/known_hosts
```

4. `.github/workflows/deploy.yml` 已创建，无需额外配置

---

## 四、常用运维命令

### 服务管理

```bash
# 启动所有服务
docker-compose up -d

# 停止所有服务
docker-compose down

# 重启某个服务
docker-compose restart api
docker-compose restart nginx
docker-compose restart mysql

# 查看服务状态
docker-compose ps
```

### 日志查看

```bash
# 查看所有日志
docker-compose logs

# 实时跟踪 API 日志
docker-compose logs -f api

# 查看最近 100 行日志
docker-compose logs --tail 100 api
```

### 进入容器调试

```bash
# 进入 API 容器
docker exec -it xblog-api bash

# 进入 MySQL 容器
docker exec -it xblog-mysql bash

# 进入 Nginx 容器
docker exec -it xblog-nginx sh

# 连接 MySQL
docker exec -it xblog-mysql mysql -uroot -pwX9xV2pJ8q
```

### 数据库操作

```bash
# 备份数据库
docker exec xblog-mysql mysqldump -uroot -pwX9xV2pJ8q xblog > backup_$(date +%Y%m%d).sql

# 恢复数据库
docker exec -i xblog-mysql mysql -uroot -pwX9xV2pJ8q xblog < backup.sql
```

### 清理重建

```bash
# 停止并删除所有容器和网络（数据卷保留）
docker-compose down

# 停止并删除所有容器、网络和数据卷（⚠️ 会删除数据库数据）
docker-compose down -v

# 完全重新构建（不使用缓存）
docker-compose build --no-cache
docker-compose up -d
```

---

## 五、问题排查

### 1. API 启动失败

```bash
docker-compose logs api
```

常见原因：
- 数据库密码错误 → 检查 `.env` 文件
- MySQL 未就绪 → API 的 `depends_on` 已配置健康检查，通常会自动等待
- 端口冲突 → `docker-compose ps` 查看端口占用

### 2. 页面无法访问

```bash
# 检查 Nginx 配置
docker exec xblog-nginx nginx -t

# 检查容器是否运行
docker-compose ps

# 检查域名解析
nslookup xblog.xiaruoxin.cn
```

### 3. 数据库连接失败

```bash
# 进入 MySQL 容器验证
docker exec -it xblog-mysql mysql -uroot -pwX9xV2pJ8q -e "SHOW DATABASES;"

# 检查数据库表是否创建成功
docker exec -it xblog-mysql mysql -uroot -pwX9xV2pJ8q xblog -e "SHOW TABLES;"
```

### 4. API 请求 404

- 确认 Nginx 的 `proxy_pass` 配置正确
- 确认 API 容器名称为 `api`（与 docker-compose 一致）
- 检查后端日志：`docker-compose logs api`

### 5. 前端页面空白

- 确认 `dist/` 目录存在且已打包
- 检查 Nginx 的 `root` 路径是否正确
- 检查 `try_files` 配置

---

## 六、安全建议

1. **修改默认密码**：生产环境务必修改 `.env` 中的数据库密码和 JWT 密钥
2. **关闭 MySQL 外部访问**：已配置仅容器内部通信，3306 端口未映射到宿主机
3. **配置 HTTPS**：使用 Let's Encrypt 免费证书
4. **关闭 Knife4j 文档**：生产环境建议在 `application.yml` 中设置 `knife4j.enable: false`
5. **定期备份**：建议设置定时任务备份数据库

---

## 七、HTTPS 配置（可选）

使用 Let's Encrypt 免费证书：

```bash
# 安装 certbot
sudo apt install certbot -y

# 获取证书
sudo certbot certonly --standalone -d xblog.xiaruoxin.cn -d xadmin.xiaruoxin.cn -d xiaruoxin.cn

# 证书文件位置
# /etc/letsencrypt/live/xblog.xiaruoxin.cn/fullchain.pem
# /etc/letsencrypt/live/xblog.xiaruoxin.cn/privkey.pem
```

修改 `docker/nginx/default.conf` 添加 HTTPS：

```nginx
server {
    listen 80;
    server_name xblog.xiaruoxin.cn;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name xblog.xiaruoxin.cn;

    ssl_certificate /etc/ssl/fullchain.pem;
    ssl_certificate_key /etc/ssl/privkey.pem;

    # ... 其余配置不变
}
```

挂载证书到 Nginx 容器（修改 `docker-compose.yml`）：

```yaml
nginx:
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./blog-web/dist:/usr/share/nginx/html/web
      - ./blog-admin/dist:/usr/share/nginx/html/admin
      - /etc/letsencrypt/live/xblog.xiaruoxin.cn:/etc/ssl:ro
```

---

## 八、GitHub Actions 自动部署（可选）

已配置 `.github/workflows/deploy.yml`，自动部署流程：

```yaml
name: Deploy to Server

on:
  push:
    branches:
      - main
      - api

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to server via SSH
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            cd /opt/xblog
            git pull origin main
            docker-compose up -d --build
```

配置 GitHub Secrets：

1. 进入 GitHub 仓库 → Settings → Secrets and variables → Actions
2. 添加以下 secrets：

| 密钥名 | 值 | 说明 |
|--------|-----|------|
| `SSH_PRIVATE_KEY` | `~/.ssh/id_rsa` 的内容 | 服务器 SSH 私钥 |
| `SERVER_HOST` | `101.34.249.12` | 服务器 IP |
| `SERVER_USER` | `ubuntu` | SSH 用户名 |

---

## 九、文件清单

| 文件 | 作用 | 是否提交 Git |
|------|------|-------------|
| `blog-api/Dockerfile` | 构建 API 镜像 | ✅ |
| `docker-compose.yml` | 编排服务 | ✅ |
| `docker/nginx/default.conf` | Nginx 反向代理 | ✅ |
| `.env` | 环境变量（密码） | ❌ 已在 .gitignore |
| `blog-admin/.env.production` | 管理端 API 地址 | ✅ |
| `blog-web/.env.production` | 展示端 API 地址 | ✅ |
| `.github/workflows/deploy.yml` | GitHub Actions 自动部署 | ✅（可选） |
