# Redis 7.x 配置
# 绑定地址（容器内无需修改，但保持灵活性）
bind 0.0.0.0

# 端口
port 6379

# 数据库数量
databases 16

# 持久化策略（生产环境建议开启AOF）
appendonly yes
appendfsync everysec

# 密码通过 docker-compose.yml command 中的 --requirepass 动态设置

# 内存策略
maxmemory 256mb
maxmemory-policy allkeys-lru

# 日志
loglevel notice